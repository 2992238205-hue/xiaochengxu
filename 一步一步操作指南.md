# 一步一步操作指南（不需要技术基础）

这份指南只做一件事：让你从 0 到可用。

---

## A. 先在你电脑上跑起来（10分钟）

### 第1步：双击启动

1. 打开这个文件夹：`/Users/yuanxi/Documents/New project`
2. 双击：`quick_start.command`

如果系统弹出权限提示，点“允许”。

### 第2步：输入两个东西

终端会问你：

1. 管理员密钥：你自己随便定一个密码（例如 `my123456`）
2. 端口：直接回车（默认 `5000`）

### 第3步：看到两个网址就是成功

终端会打印类似：

1. `Reader URL : http://192.168.x.x:5000/`
2. `Admin URL  : http://192.168.x.x:5000/admin`

这就表示服务启动成功。

---

## B. 你自己上传书（后台）

### 第1步：打开后台

在电脑浏览器打开终端打印的 `Admin URL`（以 `/admin` 结尾）。

### 第2步：上传

在后台页面里：

1. 管理员密钥：输入你刚刚设的密码（例如 `my123456`）
2. 选择书籍文件：`.md` 或 `.json`
3. 如需章节闯关任务，在“章节闯关任务”框输入（推荐）：

```text
### 1章单词表
focus, logic, memory

### 2章单词表
purpose, feature, system
```

也兼容旧格式：

```text
1|focus|专注
1|logic|逻辑
2|memory|记忆
```

4. 点击“上传并发布”

### 第3步：看到“发布成功”

日志显示“发布成功”就完成了。

---

## C. 读者怎么进入（最简单）

### 同一 Wi-Fi 下测试

1. 手机和你电脑连同一个 Wi-Fi
2. 手机浏览器打开终端里的 `Reader URL`
3. 就可以直接阅读（读者端不能上传）

---

## D. 管理员密钥到底是什么？

它不是系统给你的固定值，是你自己设置的“后台密码”。

1. 你在启动时输入什么（例如 `my123456`）
2. 后台上传时就输入同样的内容
3. 输入错了就不能上传

---

## E. 让“任何人都能进入”（上线到公网）

本地 URL 只能局域网使用。要让全国用户都能进，需要上线到云服务器。

最简单路线：Render。

### 第1步：准备 GitHub 仓库

在终端执行：

```bash
cd '/Users/yuanxi/Documents/New project'
git init
git add .
git commit -m "reader app"
```

然后在 GitHub 新建一个仓库，把它 push 上去（GitHub 网页会给你命令，照抄即可）。

### 第2步：Render 部署

1. 打开 Render，登录
2. 新建 `Web Service`
3. 连接你的 GitHub 仓库
4. 填写：
   1. Build Command: `pip install -r backend/requirements.txt`
   2. Start Command: `python backend/server.py`
5. 环境变量新增：
   1. `ADMIN_KEY` = 你自己的后台密码
6. 点 Deploy

部署成功后你会拿到一个 HTTPS 地址，例如：

`https://your-app.onrender.com`

---

## F. 小程序部署（最省事方式）

项目里已经有小程序壳：`/Users/yuanxi/Documents/New project/miniprogram`

它本质是“用小程序打开你的线上阅读网址”。

### 第1步：先确保你有线上 HTTPS 地址

必须先完成上面的 Render 部署。

### 第2步：改小程序里的网站地址

打开文件：

`/Users/yuanxi/Documents/New project/miniprogram/pages/index/index.js`

把：

`https://your-reader-domain.example/`

改成你的线上地址（例如 `https://your-app.onrender.com/`）。

### 第3步：微信公众平台配置业务域名

1. 登录你的小程序后台
2. 找到“开发管理/开发设置/业务域名”
3. 添加你的线上域名（不带路径）

### 第4步：微信开发者工具上传

1. 打开微信开发者工具
2. 导入目录：`/Users/yuanxi/Documents/New project/miniprogram`
3. 预览确认没问题
4. 点击“上传”
5. 去小程序后台提交审核并发布

发布后，用户就可以通过小程序搜索/扫码进入。

---

## G. 你现在最该做的顺序（建议）

1. 先双击 `quick_start.command` 跑通本地
2. 去 `/admin` 上传一本书，手机验证可读
3. 再做 Render 上线
4. 最后做小程序壳发布
